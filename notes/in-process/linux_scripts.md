# Initializing a new project:
@working diectory...
project_name = 
mkdir project_name
cd project_name
git init
echo "# project_name" >> README.md
touch LICENSE.md
touch .gitignore

# Quick remote commits
git add .
git commit -m " "
git remote push origin main

# Scaffolding

# Full-Stack (MERN) - multiple pages



# Full Pythonic Stack - multiple pages

mkdir project_name
cd project_name
mkdir backend
mkdir frontend
touch docker-compose.yml
cd backend
echo "if __name__ == "__main__":" >> main.py
echo "    main()" >> main.py
